package main

// 顺时针打印矩阵
func spiralOrder(matrix [][]int) []int {
	totalDis := len(matrix) * len(matrix) //总路程
	isVisited := make([][]int, len(matrix))
	j := 0
	k := 0
	currentDis := 0
	for currentDis< totalDis {
		//选一个方向 向右
		if 
	}
}
func move(dir string, x, y int) {
	if dir == "up" {
		return
	}
}
